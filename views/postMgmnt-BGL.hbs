<nav class="navbar navbar-bgl">
    <div class="container">
        <a class="navbar-brand">
            <img src= "/images/logo.png" alt = "CHAI-Taguig Logo" id = "logo-pic">
        </a>

        <a href="/admin/menuCreateBGLPost" class="centralhub-createPost"><i class="bi bi-pencil-square"></i>Create New BGL Post</a>

        <form method="GET" action="/centralhub">
            <button class="centralhub-logout" type="submit"><i class="bi bi-box-arrow-in-left"></i> BACK</button>
        </form>
    </div>
</nav>

<div class="container-events-bgl">
    <h2></h2>
    <h1>BGL Event Post Management</h1>
    <h5>Click on a post title to open it's edit page</h5>
    <div class="container-events-main-bgl">
        {{#each bglEvents}}
        <div class="card card-bgl-events" id = "all-events">
            <img src="{{this.image}}" class="card-img-top" alt="{{this.image}}">
            <div class="card-body">
                <h5 class="card-title-edit"><a href="/admin/editBGLPost/{{this._id}}" class="nav-link-postMgmt">{{this.title}}</a></h5>
                <div class="card-info">
                    <p class="card-beneficiary">Batang Gift of Love</p>
                    <p class="card-date"><i class="bi bi-calendar-fill"></i> {{this.date}}</p>
                </div>
                <p class="card-text">{{this.description}}</p>
            </div>
        </div>
        {{/each}}
    </div>
      <button id="viewMoreBtn" class="btn btn-success mt-4">View More</button>

</div>

<script>
document.addEventListener("DOMContentLoaded", () => {
  const viewMoreBtn = document.getElementById("viewMoreBtn");
  const container = document.querySelector(".container-events-bgl");
  const cards = [...document.querySelectorAll(".card-bgl-events")];

  cards.forEach((card, index) => {
    if (index < 6) {
      card.classList.add("visible");
    }
  });

  viewMoreBtn.addEventListener("click", () => {
    cards.forEach(card => card.classList.add("visible"));
    container.classList.add("expanded");
    viewMoreBtn.style.display = "none";
  });
});
</script>